const SearchBox = () => {
  return (
    <div>
      <h2>Search</h2>

      <input
        type="text"
        placeholder="Search products"
      />

      <button>Search</button>
    </div>
  );
};

export default SearchBox;